apiVersion: v1
kind: Service
metadata:
  name: frontend-fecha
  labels:
    app: frontend
spec:
  selector:
    app: frontend
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80